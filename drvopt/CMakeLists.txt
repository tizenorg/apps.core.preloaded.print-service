CMAKE_MINIMUM_REQUIRED(VERSION 2.6)
PROJECT(drvopt)
SET(PACKAGE_NAME print-service)

SET(PREFIX /usr)
SET(BINDIR "${PREFIX}/bin")
SET(PPDEXTRACTOR getdrv)

INSTALL(FILES ${PPDEXTRACTOR} DESTINATION ${BINDIR})
